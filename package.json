{
  "name": "iut-notes-parser",
  "version": "1.0.0",
  "description": "Parser for notes of IUT \"A\" Toulouse",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "run:watch": "DEBUG=* nodemon --ignore src/ dist/index.js",
    "test": "yarn run lint && yarn run build",
    "jest": "jest",
    "lint": "tslint -p tsconfig.json",
    "build": "tsc",
    "build:watch": "tsc -w"
  },
  "devDependencies": {
    "@types/node": "^13.5.1",
    "nodemon": "^2.0.2",
    "tslint": "^6.0.0",
    "tslint-config-airbnb": "^5.11.2",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "zombie": "^6.1.4"
  },
  "repository": "git@github.com:iverly/iut-notes-parser.git",
  "author": "iverly <iverly.dev@gmail.com>",
  "license": "MIT"
}
